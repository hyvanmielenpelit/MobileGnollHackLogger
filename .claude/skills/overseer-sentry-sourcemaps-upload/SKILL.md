---
name: overseer-sentry-sourcemaps-upload
description: Instructions and guidelines for building, injecting Debug IDs, and uploading Overseer's Angular source maps and debug files to Sentry using sentry-cli. Triggered when requested to "upload source maps to Sentry", "upload Overseer debug files", "upload sourcemaps", or similar for the Overseer project.
---

The full skill lives in this repository's tool-neutral agent directory (`.agents/`),
which is shared with other AI coding agents. This file is only a pointer.

Read `.agents/skills/overseer_sentry_sourcemaps_upload/SKILL.md` (path relative to the repository root) in full
before proceeding, and follow it. Any `references/` files it links are relative to that
same directory.

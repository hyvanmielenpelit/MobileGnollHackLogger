---
name: chat-retention-architecture
description: Comprehensive specification and architectural documentation of Overseer's chat message and data retention strategy, user session quotas, soft-delete lifecycle, tool call payload pruning, disk attachment cleanup, and automated database maintenance.
---

The full skill lives in this repository's tool-neutral agent directory (`.agents/`),
which is shared with other AI coding agents. This file is only a pointer.

Read `.agents/skills/chat_retention_architecture/SKILL.md` (path relative to the repository root) in full
before proceeding, and follow it. Any `references/` files it links are relative to that
same directory.

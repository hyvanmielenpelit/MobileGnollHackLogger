---
name: overseer-chat-message-handling
description: Guidelines and specifications for Overseer chat message formatting, thinking tags (<div class="ai-thought">), and clipboard copy behavior.
---

The full skill lives in this repository's tool-neutral agent directory (`.agents/`),
which is shared with other AI coding agents. This file is only a pointer.

Read `.agents/skills/overseer_chat_message_handling/SKILL.md` (path relative to the repository root) in full
before proceeding, and follow it. Any `references/` files it links are relative to that
same directory.
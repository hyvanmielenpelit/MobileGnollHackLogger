---
name: overseer-changelog
description: Instructions and guidelines for autonomously generating and appending a changelog entry to the Overseer/Data/release-notes.json file. Triggered when requested to "update the changelog", "add release notes", "generate changelog entry", or similar for the Overseer project.
---

The full skill lives in this repository's tool-neutral agent directory (`.agents/`),
which is shared with other AI coding agents. This file is only a pointer.

Read `.agents/skills/overseer_changelog/SKILL.md` (path relative to the repository root) in full
before proceeding, and follow it. Any `references/` files it links are relative to that
same directory.

@page
@model MobileGnollHackLogger.Pages.BonesTransactionsModel
@{
    ViewData["Title"] = Model.Title;
    ViewData["ContainerClass"] = "xlMaxWidth";
    ViewData["MainClass"] = "xlMaxWidth";
}

<div class="rpg-header mt-3">
    <div class="ornament"></div>
    <h1 class="mb-0" style="font-family: 'Cinzel', serif;">Bones Sharing</h1>
    <div class="ornament"></div>
</div>
@if (Model.CompatibilityVersions != null && Model.CompatibilityVersions.Count > 0)
{
    <div id="divCompatibility" class="d-flex flex-wrap justify-content-center gap-2 gap-md-4 mb-4 mt-3">
        <div class="text-center p-2 rounded" style="background: rgba(15, 20, 30, 0.6); border: 1px solid rgba(0, 240, 255, 0.1);">
            <form method="get" id="compatibilityForm" style="display: flex; align-items: center; gap: 10px;">
                <label class="text-uppercase text-muted mb-0" style="white-space: nowrap; font-size: 0.75rem; letter-spacing: 1px;"><i class="bi bi-filter-circle me-1"></i>Compatible With</label>
                <select name="compatibility" class="form-select form-select-sm rpg-select-cyan" onchange="this.form.submit()" style="width: auto;">
                    @for (int i = 0; i < Model.CompatibilityVersions.Count; i++)
                    {
                        <option value="@i" selected="@(Model.SelectedCompatibilityVersion == i ? "selected" : null)">@Model.CompatibilityVersions[i].Label</option>
                    }
                </select>
            </form>
        </div>
    </div>
}

<div class="text-center rpg-subtitle">
    BY DIFFICULTY
</div>

<ul class="nav nav-tabs rpg-tabs" id="bonesTab" role="tablist">
    <li class="nav-item" role="presentation">
        <button class="nav-link active" id="tab-all" data-bs-toggle="tab" data-bs-target="#tab-pane-all" type="button" role="tab" aria-controls="tab-pane-all" aria-selected="true">All</button>
    </li>
@foreach(var kvp in GnollHackHelper.Difficulties)
{
        int btDifficulty = kvp.Key + 4;
        <li class="nav-item" role="presentation">
            <button class="nav-link" id="tab-@(btDifficulty)" data-bs-toggle="tab" data-bs-target="#tab-pane-@(btDifficulty)" type="button" role="tab" aria-controls="tab-pane-@(btDifficulty)" aria-selected="false">@kvp.Value</button>
        </li>
}
</ul>

<div class="tab-content" id="bonesTabContent">
    <div class="tab-pane tab-padding fade show active" id="tab-pane-all" role="tabpanel" aria-labelledby="tab-all" tabindex="0">
        <h2 class="text-center">All Difficulties</h2>
        @{
            var bonesQuery = Model.DbContext.Bones.AsQueryable();
            if (Model.MinVersion.HasValue)
            {
                bonesQuery = bonesQuery.Where(b => b.VersionNumber >= Model.MinVersion.Value);
            }
            if (Model.MaxVersion.HasValue)
            {
                bonesQuery = bonesQuery.Where(b => b.VersionNumber <= Model.MaxVersion.Value);
            }
            var numBones = bonesQuery.Count();
        }
        <div class="rpg-leaderboard-card">
            <h5 class="text-center mb-3 text-white">@numBones Bones Available</h5>
            <div class="table-responsive">
                <table id="btTableAll" class="rpg-table" style="display:none;">
                <thead>
                    <tr>
                        <th>Account Name</th>
                        <th class="text-center">Bones Uploaded</th>
                        <th class="text-center">Bones Downloaded</th>
                        <th class="text-center">Bones Shared</th>
                    </tr>
                </thead>
                <tbody>
                    @{
                        var transactionsQuery = Model.DbContext.BonesTransactions.AsQueryable();
                        if (Model.MinVersion.HasValue)
                        {
                            transactionsQuery = transactionsQuery.Where(bt => bt.VersionNumber >= Model.MinVersion.Value);
                        }
                        if (Model.MaxVersion.HasValue)
                        {
                            transactionsQuery = transactionsQuery.Where(bt => bt.VersionNumber <= Model.MaxVersion.Value);
                        }

                        var queryAll = from u in Model.DbContext.Users
                                    join bt in transactionsQuery
                                    on u.Id equals bt.AspNetUserId into g
                                    orderby u.NormalizedUserName
                                    select new
                                    {
                                        User = (ApplicationUser)u,
                                        Uploads = g.Where(bt => bt.Type == TransactionType.Upload).Count(),
                                        Downloads = g.Where(bt => bt.Type == TransactionType.Download).Count(),
                                        Deletions = g.Where(bt => bt.Type == TransactionType.Deletion).Count()
                                    };
                    }
                    @foreach (var grouping in queryAll)
                    {
                        if (grouping.Uploads > 0 || grouping.Downloads > 0 || grouping.Deletions > 0)
                        {
                            <tr>
                                <td data-label="Account Name">
                                    <div class="text-white fw-bold text-uppercase" style="letter-spacing: 0.5px; font-size: 1.05rem;">@grouping.User.UserName</div>
                                </td>
                                <td class="text-center" data-label="Bones Uploaded">
                                    <span class="score-text">@grouping.Uploads</span>
                                </td>
                                <td class="text-center" data-label="Bones Downloaded">
                                    <span class="score-text">@grouping.Downloads</span>
                                </td>
                                <td class="text-center" data-label="Bones Shared">
                                    <span class="score-text">@grouping.Deletions</span>
                                </td>
                            </tr>
                        }
                    }
                </tbody>
            </table>
        </div>
        </div>
    </div>

@foreach(var kvp in GnollHackHelper.Difficulties)
{
    int btDifficulty = kvp.Key + 4;

    var difficultyTransactionsQuery = Model.DbContext.BonesTransactions.AsQueryable();
    if (Model.MinVersion.HasValue)
    {
        difficultyTransactionsQuery = difficultyTransactionsQuery.Where(bt => bt.VersionNumber >= Model.MinVersion.Value);
    }
    if (Model.MaxVersion.HasValue)
    {
        difficultyTransactionsQuery = difficultyTransactionsQuery.Where(bt => bt.VersionNumber <= Model.MaxVersion.Value);
    }

    var query = from u in Model.DbContext.Users
                join bt in difficultyTransactionsQuery
                on u.Id equals bt.AspNetUserId into g
                orderby u.NormalizedUserName
                select new
                {
                    User = (ApplicationUser)u,
                    Uploads = g.Where(bt => bt.Type == TransactionType.Upload && bt.DifficultyLevel == btDifficulty).Count(),
                    Downloads = g.Where(bt => bt.Type == TransactionType.Download && bt.DifficultyLevel == btDifficulty).Count(),
                    Deletions = g.Where(bt => bt.Type == TransactionType.Deletion && bt.DifficultyLevel == btDifficulty).Count()
                };

        <div class="tab-pane tab-padding fade" id="tab-pane-@(btDifficulty)" role="tabpanel" aria-labelledby="tab-@(btDifficulty)" tabindex="0">
            <h2 class="text-center">@kvp.Value Difficulty</h2>
            @{
                var bonesDiffQuery = Model.DbContext.Bones.Where(b => b.DifficultyLevel == btDifficulty);
                if (Model.MinVersion.HasValue)
                {
                    bonesDiffQuery = bonesDiffQuery.Where(b => b.VersionNumber >= Model.MinVersion.Value);
                }
                if (Model.MaxVersion.HasValue)
                {
                    bonesDiffQuery = bonesDiffQuery.Where(b => b.VersionNumber <= Model.MaxVersion.Value);
                }
                var numBonesDiff = bonesDiffQuery.Count();
            }
            <div class="rpg-leaderboard-card">
            <h5 class="text-center mb-3 text-white">@numBonesDiff Bones Available</h5>
            <div class="table-responsive">
            <table id="btTable@(btDifficulty)" class="rpg-table" style="display:none;">
                <thead>
                    <tr>
                        <th>Account Name</th>
                        <th class="text-center">Bones Uploaded</th>
                        <th class="text-center">Bones Downloaded</th>
                        <th class="text-center">Bones Shared</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var grouping in query)
                    {
                        if (grouping.Uploads > 0 || grouping.Downloads > 0 || grouping.Deletions > 0)
                        {
                            <tr>
                                <td data-label="Account Name">
                                    <div class="text-white fw-bold text-uppercase" style="letter-spacing: 0.5px; font-size: 1.05rem;">@grouping.User.UserName</div>
                                </td>
                                <td class="text-center" data-label="Bones Uploaded">
                                    <span class="score-text">@grouping.Uploads</span>
                                </td>
                                <td class="text-center" data-label="Bones Downloaded">
                                    <span class="score-text">@grouping.Downloads</span>
                                </td>
                                <td class="text-center" data-label="Bones Shared">
                                    <span class="score-text">@grouping.Deletions</span>
                                </td>
                            </tr>
                        }
                    }
                </tbody>
            </table>
            </div>
        </div>
    </div>
}
</div>

@section Styles
{
    <link rel="stylesheet" href="~/lib/datatables/dist/datatables.min.css" asp-append-version="true" />
}

@section Scripts
{
    <script src="~/lib/datatables/dist/datatables.min.js"></script>
    <script>
        $(".rpg-table").dataTable({
            order: [],
            autoWidth: false
        }).show();
    </script>
}
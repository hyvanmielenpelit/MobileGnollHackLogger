@page
@model JunetHackModel
@{
    ViewData["Title"] = "Junethack";
    ViewData["ActivePage"] = ManageNavPages.JunetHack;
}

<div class="login-box manage-login-box">
    <form id="profile-form" method="post" class="login-form disable-button-on-submit">
        <div class="login-form-inner">
            <div class="login-header text-center">
                <h3 class="hero-title">@ViewData["Title"]</h3>
                <div class="ornament"></div>
            </div>

            <partial name="_StatusMessage" for="StatusMessage" />

            <div asp-validation-summary="ModelOnly" class="text-danger" role="alert"></div>
            
            <div class="input-group mb-4 login-input-group">
                <span class="input-group-text" title="Junethack Username"><i class="bi bi-person-fill"></i></span>
                <input asp-for="Input.JunetHackUserName" class="form-control" placeholder="Junethack Username" title="Junethack Username" />
            </div>
            <span asp-validation-for="Input.JunetHackUserName" class="text-danger mb-3 d-block"></span>

            <div class="login-button-container mb-3 mt-4">
                <button id="update-profile-button" type="submit" class="btn btn-fantasy-login w-100">
                    <span>SAVE</span>
                </button>
            </div>
        </div>
    </form>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
